# {{name}} Backend API

Simple Express API with `/health` endpoint for GitOps demo.

## Usage

```sh
npm install
npm start
```

## Health Check

GET `/health` → `{ status: "ok", version: "1.0.0" }`
